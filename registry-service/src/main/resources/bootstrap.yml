spring:
  application:
    name: registry-service
  cloud:
    config:
      uri: http://localhost:8081
      username: root
      password: secret0
      enabled: true
  config:
    name: registry-service
    import-check: true
