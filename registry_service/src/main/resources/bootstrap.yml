spring:
  application:
    name: registry-service
  cloud:
    config:
      uri: http://localhost:8081
      username: root
      password: secret0
  config:
    name: registry-service
    import-check: true